# Plan

Current bridge is graph-first, Agent Memory-aware, and stateless. It exposes health, datasets, bounded graph inspection, evidence reads, reviewed document upload, and native Agent Memory retrieval/recording through one MCP stdio server for Claude Code, OpenCode, and Hermes.

Deferred: semantic retrieval, session APIs, destructive graph operations, analytics refresh, HTTP MCP transport, and automatic conversation ingestion.
