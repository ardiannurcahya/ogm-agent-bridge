# Plan

Current bridge is graph-first and stateless. It exposes health, datasets, bounded graph inspection, evidence reads, and reviewed document upload through one MCP stdio server for Claude Code, OpenCode, and Hermes.

Deferred: semantic retrieval, memory/session APIs, destructive graph operations, analytics refresh, HTTP MCP transport, and automatic conversation ingestion.
